import 'package:nitrogen_types/nitrogen_types.dart';

// GENERATED CODE - DO NOT MODIFY BY HAND
//
// **************************************************************************
// nitrogen
// **************************************************************************
//
// ignore_for_file: type=lint
// ignore_for_file: deprecated_member_use

/// Contains the assets and nested directories in the `assets` directory.
///
/// Besides the assets and nested directories, it also provides a [contents] for querying the assets in the current
/// directory.
///
/// To convert an asset into a widget, [call the asset like a function](https://dart.dev/language/callable-objects].
/// ```dart
/// final widget = TAssets.path.to.asset();
/// ```
///
/// The `call(...)` functions are provided by extensions. By default, only the [ImageAsset] extension is bundled.
///
/// 3rd party packages are supported via 'extension' packages. `extension` packages contain an `extension` that provide a
/// `call(...)` function that transforms an `Asset` into a 3rd party type.
///
/// | Type              | Package       | Extension Package      | Version                                                                                                        |
/// |-------------------|---------------|------------------------|----------------------------------------------------------------------------------------------------------------|
/// | SVG images        | `flutter_svg` | `nitrogen_flutter_svg` | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_flutter_svg)](https://pub.dev/packages/nitrogen_flutter_svg) |
/// | Lottie animations | `lottie`      | `nitrogen_lottie`      | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_lottie)](https://pub.dev/packages/nitrogen_lottie)           |
class TAssets {
  const TAssets();

  /// The `assets/icons` directory.
  static $TAssetsIcons get icons => const $TAssetsIcons();

  /// The `assets/images` directory.
  static $TAssetsImages get images => const $TAssetsImages();

  /// The contents of this directory.
  static Map<String, Asset> get contents => const {};

  /// The toast of this directory.
  static Map<String, Asset> get toast => const {};
}

/// Contains the assets and nested directories in the `assets/icons` directory.
///
/// Besides the assets and nested directories, it also provides a [contents] for querying the assets in the current
/// directory.
///
/// To convert an asset into a widget, [call the asset like a function](https://dart.dev/language/callable-objects].
/// ```dart
/// final widget = $TAssetsIcons.path.to.asset();
/// ```
///
/// The `call(...)` functions are provided by extensions. By default, only the [ImageAsset] extension is bundled.
///
/// 3rd party packages are supported via 'extension' packages. `extension` packages contain an `extension` that provide a
/// `call(...)` function that transforms an `Asset` into a 3rd party type.
///
/// | Type              | Package       | Extension Package      | Version                                                                                                        |
/// |-------------------|---------------|------------------------|----------------------------------------------------------------------------------------------------------------|
/// | SVG images        | `flutter_svg` | `nitrogen_flutter_svg` | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_flutter_svg)](https://pub.dev/packages/nitrogen_flutter_svg) |
/// | Lottie animations | `lottie`      | `nitrogen_lottie`      | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_lottie)](https://pub.dev/packages/nitrogen_lottie)           |
class $TAssetsIcons {
  const $TAssetsIcons();

  /// The `assets/icons/arrow_down.svg`.
  SvgAsset get arrowDown => const SvgAsset(
        'design_systems',
        'arrow_down',
        'assets/icons/arrow_down.svg',
      );

  /// The `assets/icons/arrow_up.svg`.
  SvgAsset get arrowUp => const SvgAsset(
        'design_systems',
        'arrow_up',
        'assets/icons/arrow_up.svg',
      );

  /// The `assets/icons/checkbox_off.svg`.
  SvgAsset get checkboxOff => const SvgAsset(
        'design_systems',
        'checkbox_off',
        'assets/icons/checkbox_off.svg',
      );

  /// The `assets/icons/checkbox_on.svg`.
  SvgAsset get checkboxOn => const SvgAsset(
        'design_systems',
        'checkbox_on',
        'assets/icons/checkbox_on.svg',
      );

  /// The `assets/icons/clock.svg`.
  SvgAsset get clock => const SvgAsset(
        'design_systems',
        'clock',
        'assets/icons/clock.svg',
      );

  /// The `assets/icons/close.svg`.
  SvgAsset get close => const SvgAsset(
        'design_systems',
        'close',
        'assets/icons/close.svg',
      );

  /// The `assets/icons/filter.svg`.
  SvgAsset get filter => const SvgAsset(
        'design_systems',
        'filter',
        'assets/icons/filter.svg',
      );

  /// The `assets/icons/letter.svg`.
  SvgAsset get letter => const SvgAsset(
        'design_systems',
        'letter',
        'assets/icons/letter.svg',
      );

  /// The `assets/icons/refresh.svg`.
  SvgAsset get refresh => const SvgAsset(
        'design_systems',
        'refresh',
        'assets/icons/refresh.svg',
      );

  /// The `assets/icons/toast_check.svg`.
  SvgAsset get toastCheck => const SvgAsset(
        'design_systems',
        'toast_check',
        'assets/icons/toast_check.svg',
      );

  /// The `assets/icons/toast_close.svg`.
  SvgAsset get toastClose => const SvgAsset(
        'design_systems',
        'toast_close',
        'assets/icons/toast_close.svg',
      );

  /// The contents of this directory.
  Map<String, Asset> get contents => const {
        'arrow_down': const SvgAsset(
          'design_systems',
          'arrow_down',
          'assets/icons/arrow_down.svg',
        ),
        'arrow_up': const SvgAsset(
          'design_systems',
          'arrow_up',
          'assets/icons/arrow_up.svg',
        ),
        'checkbox_off': const SvgAsset(
          'design_systems',
          'checkbox_off',
          'assets/icons/checkbox_off.svg',
        ),
        'checkbox_on': const SvgAsset(
          'design_systems',
          'checkbox_on',
          'assets/icons/checkbox_on.svg',
        ),
        'clock': const SvgAsset(
          'design_systems',
          'clock',
          'assets/icons/clock.svg',
        ),
        'close': const SvgAsset(
          'design_systems',
          'close',
          'assets/icons/close.svg',
        ),
        'filter': const SvgAsset(
          'design_systems',
          'filter',
          'assets/icons/filter.svg',
        ),
        'letter': const SvgAsset(
          'design_systems',
          'letter',
          'assets/icons/letter.svg',
        ),
        'refresh': const SvgAsset(
          'design_systems',
          'refresh',
          'assets/icons/refresh.svg',
        ),
        'apple': const SvgAsset(
          'design_systems',
          'apple',
          'assets/icons/apple.svg',
        ),
      };
}

/// Contains the assets and nested directories in the `assets/images` directory.
///
/// Besides the assets and nested directories, it also provides a [contents] for querying the assets in the current
/// directory.
///
/// To convert an asset into a widget, [call the asset like a function](https://dart.dev/language/callable-objects].
/// ```dart
/// final widget = $TAssetsImages.path.to.asset();
/// ```
///
/// The `call(...)` functions are provided by extensions. By default, only the [ImageAsset] extension is bundled.
///
/// 3rd party packages are supported via 'extension' packages. `extension` packages contain an `extension` that provide a
/// `call(...)` function that transforms an `Asset` into a 3rd party type.
///
/// | Type              | Package       | Extension Package      | Version                                                                                                        |
/// |-------------------|---------------|------------------------|----------------------------------------------------------------------------------------------------------------|
/// | SVG images        | `flutter_svg` | `nitrogen_flutter_svg` | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_flutter_svg)](https://pub.dev/packages/nitrogen_flutter_svg) |
/// | Lottie animations | `lottie`      | `nitrogen_lottie`      | [![Pub Dev](https://img.shields.io/pub/v/nitrogen_lottie)](https://pub.dev/packages/nitrogen_lottie)           |
class $TAssetsImages {
  const $TAssetsImages();

  /// The `assets/images/clap.png`.
  ImageAsset get clap => const ImageAsset(
        'design_systems',
        'clap',
        'assets/images/clap.png',
      );

  /// The contents of this directory.
  Map<String, Asset> get contents => const {
        'clap': const ImageAsset(
          'design_systems',
          'clap',
          'assets/images/clap.png',
        ),
      };
}
